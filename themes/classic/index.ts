// Classic theme is the original layout that lived in components/public/*.
// It is preserved here so we can hot-swap it back via NEXT_PUBLIC_SITE_THEME=classic.

export { SiteHeader } from "@/components/public/site-header";
export { SiteFooter } from "@/components/public/site-footer";
export { HomePage } from "./home-page";
export { config } from "./config";
